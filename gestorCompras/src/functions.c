//añadir fecha actual
time_t now = time(0);
char* dt = ctime(&now);

//nueva compra
void newBuy(){
	printf("Introducir nombre del comprador (aitor/paula):\n");
	string buyer;
	scanf("%s", buyer);
	if ()
}